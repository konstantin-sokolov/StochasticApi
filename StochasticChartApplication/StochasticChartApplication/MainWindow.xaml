<Window x:Class="StochasticChartApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Stochastic events" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>
        <Button Margin="10" Width="100" Height="30" Grid.Row="1" Content="Calculate" 
                Command="{Binding GenerateDataCommand}"/>
        <ContentPresenter Content="{Binding EventDensityViewModel}">
        </ContentPresenter>
    </Grid>
</Window>
